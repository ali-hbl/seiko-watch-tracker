# seiko-watch-tracker

This project runs a scheduled job (cron) every day at 11:00 to check several online stores for a Seiko watch I like. It collects the current availability and price, then sends me a Telegram message if the watch is in stock, so I don’t have to check manually.
